# include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter the first number:");
    scanf("%d",&a);
    printf("Enter the Second number:");
    scanf("%d",&b);
    sum = a+b;
    printf("The sum of two number is:%d",sum);
    return 0;
}